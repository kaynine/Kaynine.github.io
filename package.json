{
  "name": "kaynine-blog",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "node -e \"try{require('fs').rmSync('.astro/data-store.json')}catch(_){}\" && astro dev",
    "build": "node -e \"try{require('fs').rmSync('.astro/data-store.json')}catch(_){}\" && astro build && node scripts/download-images.mjs",
    "preview": "astro preview"
  },
  "overrides": {
    "@notionhq/client": "^5.22.0"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "glob": "^13.0.6",
    "notion-astro-loader": "^0.4.0"
  }
}
